# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

install! 'cocoapods', :disable_input_output_paths => true
install! 'cocoapods',generate_multiple_pod_projects: true

target 'SwiftResource' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!
  pod 'ModuleA', :path => './Module/ModuleA/ModuleA'
  pod 'ModuleB', :path => './Module/ModuleB/ModuleB'
  pod 'ModuleC', :path => './Module/ModuleC/ModuleC'
  pod 'ModuleD', :path => './Module/ModuleD/ModuleD'
  pod 'ResourceExt', :path => './Module/ResourceExt/ResourceExt'
end
